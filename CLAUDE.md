# Toki

## Reference

- `onecli/` contains a reference implementation — use it for patterns around credential injection (MITM proxy, AES-256-GCM encryption, host/path matching, header injection). Don't copy directly; adapt the architecture to Toki's needs.
