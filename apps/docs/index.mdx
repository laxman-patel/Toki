# Toki

Toki is an agent-first control plane for secure tool access.

The initial MVP is intentionally narrow:

- Bun + Hono backend
- TypeScript CLI
- Toki-native runtime policy engine
- short-lived leases
- action-level audit trail

Use Infisical as a reference, not as Toki's runtime architecture.
